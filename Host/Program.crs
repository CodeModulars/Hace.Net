# 获取项目名称参数
@call $pro_name get_argument("project")
# 设定输出路径
@render "$(pro_name).Host\\Program.cs"
using $(pro_name).Host;
using System.Diagnostics;
@line
// 注册日志
sy.Logger.Factory.UseStringAction(message => Debug.WriteLine(message));
sy.Logger.Create();
// 运行主机服务
sy.Hosting.CreateWebApplication<Startup>(args).Run();